# Copy this file to .env and replace the placeholder with your real PostgreSQL URL.
# Never commit .env or paste the real value into source code.
DATABASE_URL=postgresql://username:password@host:5432/database?sslmode=require

# Optional for local PostgreSQL without SSL. Defaults to SSL enabled.
# DATABASE_SSL=false

PORT=3000